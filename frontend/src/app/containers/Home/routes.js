import { Home } from './';

const routeKeys = {
  HOME: 'HOME'
};
const routes = {
  HOME: {
    path: '/',
  }
};

const componentMap = {
  HOME: 'Home'
};

const components = [Home];

export { routes, componentMap, routeKeys, components };
